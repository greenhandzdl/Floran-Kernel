# Floran Kernel - Docker btrfs 存储驱动支持（可独立关闭以二分 bootloop）
# BTRFS 仅在使用 btrfs storage driver 时才需要；Docker 默认的 overlay2 /
# fuse-overlayfs 不需要它。由 docker_fs 开关独立控制（仅 docker=true 时生效）。

CONFIG_BTRFS_FS=y
CONFIG_BTRFS_FS_POSIX_ACL=y
