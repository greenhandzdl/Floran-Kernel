# Floran Kernel - Docker CPU 带宽控制（CFS_BANDWIDTH）—— 本设备已证实会 bootloop，默认不启用
# 由 docker_cgroup_core 开关控制，默认 false。
#
# ===== bootloop 定案（sm8650/pineapple，GKI 6.1）=====
# 单变量对照实验结论：
#   K2 (只 USER_NS)                    -> 正常开机
#   K3 (FAIR_GROUP_SCHED + CFS_BANDWIDTH) -> 卡第一屏
#   K1 (USER_NS + FAIR + CFS)          -> 卡第一屏
# 差集锁定 K3 的增量为 CFS_BANDWIDTH（FAIR_GROUP_SCHED 是 CGROUP_SCHED=y 的
# default，基线本就 effective=y，属 no-op；CFS_BANDWIDTH 基线=n，是唯一真实改动）。
#
# 机理：CONFIG_CFS_BANDWIDTH=y 向 struct task_group / cfs_rq / sched_entity 追加带宽
# 字段，改变这些被 vendor 模块与 boot 早期组件按偏移内嵌引用的核心调度结构体布局，
# 导致高通数据面/调度相关模块 probe 错乱 -> init 阶段卡第一屏。
# 代价：失去 docker --cpus / cgroup v2 cpu.max 硬配额；容器仍可用 cpu.weight(cgroup v2)
# 做相对权重，且 MEMCG/CPUSETS/CGROUP_SCHED 都在安全底座里，内存与亲和限制正常。
# 因此默认不启用；若将来 KMI 对齐后可手动开 docker_cgroup_core=true。
#
# CONFIG_CFS_BANDWIDTH=y   # 已证实 bootloop，保持注释
# CONFIG_FAIR_GROUP_SCHED=y  # no-op（CGROUP_SCHED 已带出），无需显式声明
