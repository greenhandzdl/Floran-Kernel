# Floran Kernel - Docker cgroup 核心必需项（runc/containerd 运行容器真正依赖的一组）
# 由 docker_cgroup_core 开关控制（仅 docker=true 且 docker_cgroup=true 时生效）。
# 这批是 Docker 功能必需：USER_NS(容器 uid 映射/rootless)、CGROUP_DEVICE(设备访问)、
# CGROUP_PIDS(--pids-limit)、CFS_BANDWIDTH(--cpus CPU 配额)、FAIR_GROUP_SCHED(CFS带宽前置)。
# 若本组单独能开机，它就是可交付的 Docker 成品内核。

CONFIG_USER_NS=y
CONFIG_CGROUP_DEVICE=y
CONFIG_CGROUP_PIDS=y
CONFIG_FAIR_GROUP_SCHED=y
CONFIG_CFS_BANDWIDTH=y
