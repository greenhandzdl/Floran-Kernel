# Floran Kernel - Docker 用户命名空间 (USER_NS) —— 单独门控以便二分 bootloop
# rootless 容器 / docker --userns-remap 需要；rootful Docker 不严格依赖。
# USER_NS 会显著改变 Android init 与命名空间/SELinux 交互，是核心簇里的重点嫌疑，
# 因此从 docker-cgroup.config 拆出，由 docker_userns 单独控制（仅 docker=true 时生效）。
CONFIG_USER_NS=y
